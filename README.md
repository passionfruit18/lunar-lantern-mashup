# 2026_02_17_Hanzi_Dash

Run pip install -r requirements.txt

Run npm install

Run npx tsc -w (maybe in a separate terminal tab) to build the game engine. Compiling Transcript to Javascript.

Run python app.py to run!