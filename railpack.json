{
    "$schema": "https://schema.railpack.com",
    "packages": {
      "node": "20"
    },
    "steps": {
      "build": {
        "commands": [
          "npm install",
          "npm run build"
        ]
      }
    },
    "deploy": {
        "startCommand": "gunicorn -k 'geventwebsocket.gunicorn.workers.GeventWebSocketWorker' -w 1 --timeout 120 --bind 0.0.0.0:$PORT app:app"
    }
  }